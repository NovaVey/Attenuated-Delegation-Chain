{
  "name": "@adc/mint-service",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP mint service: holds the root key, consults RBA to bound a root token's scope before minting. See docs/PLAN.md Phase 4.",
  "type": "module",
  "scripts": {
    "prebuild": "npm run build -w @adc/core --if-present && npm run build -w @adc/revocation --if-present",
    "build": "tsc -p tsconfig.json",
    "pretest": "npm run build",
    "test": "node --test dist/test/*.test.js",
    "pretypecheck": "npm run build -w @adc/core --if-present && npm run build -w @adc/revocation --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/src/index.js"
  },
  "dependencies": {
    "@adc/core": "*",
    "@adc/revocation": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "license": "MIT"
}
